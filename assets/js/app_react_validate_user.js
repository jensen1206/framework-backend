import * as React from 'react';
import ReactDOM from "react-dom/client";
import App from "./App/AccountValidate/App.jsx";
let target = document.getElementById("validate-user-react-app");
const el = ReactDOM.createRoot(target);
el.render(
    <App/>
);